{
  "name": "ewissouq",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n front,back -c cyan,magenta \"yarn dev:front\" \"yarn dev:backend\"",
    "dev:front": "nuxt dev",
    "dev:backend": "yarn --cwd backend dev",
    "dev:clean": "nuxt cleanup && nuxt prepare && yarn dev",
    "seed:backend": "yarn --cwd backend seed",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@nuxtjs/i18n": "^9.3.0",
    "@pinia/nuxt": "^0.9.0",
    "@vueuse/core": "^12.0.0",
    "@vueuse/nuxt": "^12.0.0",
    "axios": "^1.7.9",
    "bootstrap": "^5.3.3",
    "bootstrap-icons": "^1.11.3",
    "js-cookie": "^3.0.5",
    "nuxt": "^3.15.0",
    "pinia": "^2.3.0",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "sass": "^1.83.0"
  }
}
